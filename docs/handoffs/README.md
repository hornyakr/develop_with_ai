# Handoffs

Generate with:

```bash
pnpm agent:handoff -- --agent <name> --task <TASK-ID> --base <commit>
```

Fill every `FILL IN` before the next writer or integrator takes over. Do not commit secrets, full sensitive prompts, or production data.
