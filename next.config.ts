import type { NextConfig } from "next"

const nextConfig: NextConfig = {
  // The course teaches explicit Stream / Cache decisions with Cache Components.
  cacheComponents: true,
}

export default nextConfig
